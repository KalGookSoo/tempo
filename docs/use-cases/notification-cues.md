# 알림 큐 유즈케이스

## 목적

사용자는 타이머 상태 변화를 소리, 진동, 음원 파일, 직접 녹음 음성으로 인지한다.

## 주요 사용자

- 화면을 계속 볼 수 없는 운동자
- 수업을 진행하는 코치
- 조용한 환경에서 진동을 선호하는 사용자

## 선행 조건

- 사용자는 알림 큐 설정에 접근할 수 있다.
- 타이머 기능은 알림 이벤트를 발생시킬 수 있다.

## 기본 흐름

1. 사용자가 알림 큐 설정을 연다.
2. 사용자가 알림 방식을 선택한다.
3. 사용자가 시작 전 알림 시점을 선택한다.
4. 사용자가 이벤트별 알림을 확인하거나 수정한다.
5. 사용자가 설정을 저장한다.
6. 타이머 실행 중 이벤트가 발생하면 알림 큐가 실행된다.

## 대안 흐름

- 사용자는 알림 방식을 없음, 사운드, 진동, 사운드 + 진동 중 선택한다.
- 사용자는 시작 전 알림을 없음, 1초, 3초, 5초, 10초 중 선택한다.
- 사용자는 후속 단계에서 음원 파일을 이벤트별로 지정할 수 있다.
- 사용자는 후속 단계에서 직접 녹음한 코치 음성을 이벤트별로 지정할 수 있다.

## Mermaid

```mermaid
%%{init: {"theme": "base", "themeVariables": {"background": "transparent", "primaryColor": "#FFFFFF", "primaryTextColor": "#111111", "primaryBorderColor": "#111111", "lineColor": "#111111", "tertiaryColor": "#F7F7F2"}}}%%
flowchart TD
  A["알림 큐 설정 열기"] --> B["알림 방식 선택"]
  B --> C["시작 전 알림 시점 선택"]
  C --> D["이벤트별 알림 설정"]
  D --> E{"커스텀 리소스 사용?"}
  E -- "기본 사운드/진동" --> F["설정 저장"]
  E -- "음원 파일" --> G["파일 선택 / 미리듣기"]
  G --> F
  E -- "직접 녹음" --> H["녹음 / 이름 지정 / 미리듣기"]
  H --> F
  F --> I["타이머 실행"]
  I --> J{"알림 이벤트 발생"}
  J -- "준비 / 운동 / 휴식 / 종료" --> K["선택한 방식으로 알림 실행"]
  K --> I

  classDef action fill:#FFF4BF,stroke:#111111,color:#111111;
  classDef done fill:#DDF8E8,stroke:#111111,color:#111111;
  class A,B,C,D,E,G,H,I action;
  class F,K done;
```

## 검수 포인트

- 알림 방식은 없음, 사운드, 진동, 사운드 + 진동을 지원한다.
- 시작 전 알림 시점은 없음, 1초, 3초, 5초, 10초를 지원한다.
- MVP는 기본 사운드와 진동을 제공한다.
- 구조는 음원 파일과 직접 녹음 음성 큐를 수용할 수 있어야 한다.

